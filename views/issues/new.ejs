<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>New Issue</title>
	</head>
	<body>
		<header>
			<h1>New Issue</h1>
		</header>
		<main>
			<a href="#" onclick="history.go(-1)">Back</a>
			<form action="<%= baseUrl %>/issues" method="post">
				<input
					type="text"
					id="title"
					name="title"
					placeholder="What needs to be done?"
					required
				/>
				<br />
				<label for="description">
					Description:
					<input type="text" id="description" name="description" />
				</label>
				<br />
				<label for="type">
					Issue type:
					<select name="type" id="type">
						<% const types = ['Story', 'Task', 'Bug', 'Epic'] %> <%
						types.forEach(type => { %>
						<option value="<%= type %>">
							<%= type %>
						</option>
						<% }) %>
					</select>
				</label>
				<br />
				<label for="reporter">
					Reporter:
					<input type="text" id="reporter" name="reporter" />
				</label>
				<br />
				<label for="assignee">
					Assignee:
					<input type="text" id="assignee" name="assignee" />
				</label>
				<br />
				<label for="priority">
					Priority:
					<select name="priority" id="priority">
						<% const priorities = ['highest', 'high', 'medium', 'low', 'lowest'] %> <%
						priorities.forEach(priority => { %>
						<option value="<%= priority %>" <% if (priority == 'medium') { %> selected <% } %>>
							<%= priority %>
						</option>
						<% }) %>
					</select>
				</label>
				<br />
				<label for="storyPoints">
					Story points:
					<input type="number" id="storyPoints" name="storyPoints" min="0"/>
				</label>
				<br />
				<label for="epic">
					Epic:
					<input type="text" id="epic" name="epic" />
				</label>
				<br />
				<input type="submit" />
			</form>
		</main>
	</body>
</html>
